import Typography from "../components/Typography";

export default function Page() {
  const style = {
    marginTop: "4em",
    marginLeft: "4em",
  };

  return (
    <div style={style}>
      <Typography>
        This app is currently being migrated. Please check back later.
      </Typography>
    </div>
  );
}
